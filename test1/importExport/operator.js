const add = (a, b) => {
    return a + b;
}

const sub = (a, b) => {
    return a - b;
}

const mult = (a, b) => {
    return a * b;
}

const name = "sahil";

// module.exports.add = add;
// module.exports.sub = sub;

module.exports = {add, sub, mult};